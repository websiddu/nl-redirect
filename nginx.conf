server {
    listen 8080;
    server_name _;

    # 301 redirect ALL requests to usenewsletters.com, preserving path + query
    location / {
        return 301 https://usenewsletters.com$request_uri;
    }
}
